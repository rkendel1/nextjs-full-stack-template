# Optional override for the local FeltDB/AppPort durable state directory.
# Leave unset to use ./.data/appport.
FELTDB_PATH=.data/appport

# Optional override when your environment uses a different Chromium binary and you
# want to run the Runora browser test locally.
CHROMIUM_PATH=/usr/bin/chromium-browser
